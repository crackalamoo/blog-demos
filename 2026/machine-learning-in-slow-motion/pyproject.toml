[project]
name = "machine-learning-in-slow-motion"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "matplotlib",
    "remotezip",
    "numba",
]
